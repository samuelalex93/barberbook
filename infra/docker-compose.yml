version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: studiobook_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: studiobook
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - studiobook_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   container_name: studiobook_pgadmin
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@studiobook.com
  #     PGADMIN_DEFAULT_PASSWORD: admin123
  #   ports:
  #     - "5050:80"
  #   depends_on:
  #     - postgres
  #   networks:
  #     - studiobook_network
  #   volumes:
  #     - pgadmin_data:/var/lib/pgadmin

volumes:
  postgres_data:
    driver: local
  # pgadmin_data:
  #   driver: local

networks:
  studiobook_network:
    driver: bridge
